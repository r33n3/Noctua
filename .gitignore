# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# Node
node_modules/

# Build artifacts (docs/ is the GitHub Pages output — keep it)
# docs/

# Secrets
.env
*.key
*.pem

# Dev session artifacts (screenshots, browser automation output)
*.png
*.jpg
*.jpeg
.playwright-mcp/

# Student profiles — local only, never commit
.noctua/

# Student instance data — local only, never commit
student-state/preferences.md
student-state/progress.md
student-state/reflection-log.md

# Internal planning docs (not student-facing)
docs/superpowers/

# Author research snapshots — not student-facing, not committed
docs/research/

# Author's local Claude Code config — students install skills from docs/skills/
.claude/

# Codex-specific agent config — used during course review, not student-facing
.agents/
AGENTS.md

# MASS deployed agent IDs (contain real Anthropic agent IDs)
tools/mass/claude-managed-agents/agent_ids.json
